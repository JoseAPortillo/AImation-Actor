[
  {
    "type": "pass-through",
    "category": "logic",
    "title": "Pass Through",
    "description": "Forwards the input value unchanged (universal relay).",
    "inputs": [
      {
        "name": "input",
        "data_type": "any",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "output",
        "data_type": "any",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": []
  },
  {
    "type": "merge",
    "category": "logic",
    "title": "Merge",
    "description": "Concatenates two frame streams into one.",
    "inputs": [
      {
        "name": "input_a",
        "data_type": "frames",
        "required": true,
        "default": null,
        "description": ""
      },
      {
        "name": "input_b",
        "data_type": "frames",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "merged",
        "data_type": "frames",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": []
  },
  {
    "type": "frame-range",
    "category": "source",
    "title": "Frame Range",
    "description": "Emits frame indices in the half-open range [start, end).",
    "inputs": [],
    "outputs": [
      {
        "name": "frames",
        "data_type": "frames",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "start",
        "data_type": "number",
        "required": true,
        "default": null,
        "description": ""
      },
      {
        "name": "end",
        "data_type": "number",
        "required": true,
        "default": null,
        "description": ""
      }
    ]
  },
  {
    "type": "video-source",
    "category": "source",
    "title": "Frame Extractor",
    "description": "Decodes a video file into frames (OpenCV).",
    "inputs": [],
    "outputs": [
      {
        "name": "frames",
        "data_type": "frames",
        "required": true,
        "default": null,
        "description": ""
      },
      {
        "name": "fps",
        "data_type": "number",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "video_path",
        "data_type": "video_path",
        "required": true,
        "default": null,
        "description": ""
      },
      {
        "name": "start",
        "data_type": "number",
        "required": true,
        "default": 0,
        "description": ""
      },
      {
        "name": "end",
        "data_type": "number",
        "required": false,
        "default": null,
        "description": ""
      },
      {
        "name": "resize",
        "data_type": "number",
        "required": false,
        "default": null,
        "description": ""
      }
    ]
  },
  {
    "type": "pose-2d",
    "category": "ai",
    "title": "Pose 2D",
    "description": "Estimate 2D keypoints from video frames",
    "inputs": [
      {
        "name": "frames",
        "data_type": "frames",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "keypoints",
        "data_type": "keypoints_2d",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "model",
        "data_type": "string",
        "required": false,
        "default": "synthetic",
        "description": "Backend model: 'synthetic' or 'onnx'"
      },
      {
        "name": "confidence",
        "data_type": "number",
        "required": false,
        "default": 0.0,
        "description": "Confidence threshold for keypoints [0, 1]"
      }
    ]
  },
  {
    "type": "pose-3d",
    "category": "ai",
    "title": "Pose 3D",
    "description": "Lift 2D keypoints into normalized 3D poses",
    "inputs": [
      {
        "name": "keypoints",
        "data_type": "keypoints_2d",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "keypoints_3d",
        "data_type": "pose_3d",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "model",
        "data_type": "string",
        "required": false,
        "default": "synthetic",
        "description": "Backend model: 'synthetic', 'heuristic' or 'onnx'"
      },
      {
        "name": "depth_mode",
        "data_type": "string",
        "required": false,
        "default": "proportional",
        "description": "Depth deviation mode: 'proportional' or 'flat'"
      },
      {
        "name": "confidence",
        "data_type": "number",
        "required": false,
        "default": 0.0,
        "description": "Confidence threshold for output keypoints [0, 1]"
      }
    ]
  },
  {
    "type": "video-to-motion",
    "category": "output",
    "title": "Video to Motion",
    "description": "Convert normalized 3D keypoints into a neutral motion",
    "inputs": [
      {
        "name": "keypoints_3d",
        "data_type": "pose_3d",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "person_height_cm",
        "data_type": "number",
        "required": false,
        "default": 172.0,
        "description": "Person height used to upscale normalized keypoints (cm)"
      },
      {
        "name": "only_local",
        "data_type": "boolean",
        "required": false,
        "default": true,
        "description": "Whether to derive local (child-parent) offsets"
      }
    ]
  },
  {
    "type": "temporal-cleanup",
    "category": "cleanup",
    "title": "Temporal Cleanup",
    "description": "Smooth motion, lock feet on contact, clamp to ground, normalize root",
    "inputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "min_cutoff",
        "data_type": "number",
        "required": false,
        "default": 1.0,
        "description": "Minimum cutoff frequency for One-Euro filter"
      },
      {
        "name": "beta",
        "data_type": "number",
        "required": false,
        "default": 0.5,
        "description": "Beta parameter for One-Euro filter (speed smoothing)"
      },
      {
        "name": "velocity_threshold",
        "data_type": "number",
        "required": false,
        "default": 5.0,
        "description": "Velocity threshold (cm/frame) for foot contact detection"
      },
      {
        "name": "height_threshold",
        "data_type": "number",
        "required": false,
        "default": 10.0,
        "description": "Height threshold (cm) for foot contact detection"
      },
      {
        "name": "hysteresis_frames",
        "data_type": "number",
        "required": false,
        "default": 4,
        "description": "Frames to maintain foot lock after contact loss"
      }
    ]
  },
  {
    "type": "inbetween-generation",
    "category": "enrichment",
    "title": "In-Between Generation",
    "description": "Resample, ease, filter rotation continuity, and smooth tangents",
    "inputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "interpolation_method",
        "data_type": "string",
        "required": false,
        "default": "cubic",
        "description": "\"linear\" or \"cubic\" interpolation"
      },
      {
        "name": "target_fps",
        "data_type": "number",
        "required": false,
        "default": 30.0,
        "description": "Target frame rate to upsample onto"
      },
      {
        "name": "easing",
        "data_type": "string",
        "required": false,
        "default": "none",
        "description": "\"none\", \"ease-in\", \"ease-out\" or \"ease-in-out\""
      },
      {
        "name": "euler_filter",
        "data_type": "boolean",
        "required": false,
        "default": true,
        "description": "Canonicalize rotation sign continuity"
      },
      {
        "name": "tangent_smoothing",
        "data_type": "number",
        "required": false,
        "default": 0.0,
        "description": "Smoothing intensity in [0, 1]"
      }
    ]
  },
  {
    "type": "retarget-map",
    "category": "rigging",
    "title": "Retarget Map",
    "description": "Map a neutral motion onto a target rig from a YAML/JSON preset",
    "inputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "outputs": [
      {
        "name": "motion",
        "data_type": "neutral_animation",
        "required": true,
        "default": null,
        "description": ""
      }
    ],
    "params": [
      {
        "name": "mapping_preset",
        "data_type": "string",
        "required": true,
        "default": null,
        "description": "Preset file name under the allowlisted preset root"
      },
      {
        "name": "use_root_translation",
        "data_type": "boolean",
        "required": false,
        "default": null,
        "description": "Override the preset use_root_translation (node wins)"
      },
      {
        "name": "foot_ik",
        "data_type": "boolean",
        "required": false,
        "default": null,
        "description": "Override the preset foot_ik (node wins)"
      },
      {
        "name": "scale_source_height",
        "data_type": "boolean",
        "required": false,
        "default": null,
        "description": "Override the preset scale_source_height (node wins)"
      },
      {
        "name": "preserve_keyframes",
        "data_type": "boolean",
        "required": false,
        "default": null,
        "description": "Override the preset preserve_keyframes (node wins)"
      }
    ]
  }
]
